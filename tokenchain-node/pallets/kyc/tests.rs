#![cfg(test)]

use super::*;
use frame_support::{assert_ok, assert_noop};

#[test]
fn verify_and_unverify_works() {
    // Squelette de test à adapter selon votre runtime
    // Vérifie puis retire la vérification d'un compte
}
